{
  "type": "record",
  "subject": "ocpi.queue.session.charge-point-assigned-key",
  "namespace": "ocpi.queue.session",
  "name": "QueueSessionKey",
  "doc": "Key for charge point assignment events, keyed by the OCPI queue session.",
  "fields": [
    {
      "name": "session_id",
      "type": "string",
      "doc": "OCPI queue session identifier (maps to QueueSession::$uid)."
    },
    {
      "name": "tenant_id",
      "type": [
        "null",
        "string"
      ],
      "default": null
    }
  ]
}
